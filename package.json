{
  "name": "good-detour",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A calm, local-first browser redirect extension.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.js",
    "package": "npm run build && cd dist/chrome && zip -qr ../good-detour-chrome.zip ."
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

